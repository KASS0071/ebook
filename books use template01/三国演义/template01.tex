\documentclass[10pt,openany]{book}
\usepackage[paperwidth=176mm, paperheight=250mm, top=25mm, bottom=25mm, left=20mm, right=20mm]{geometry}
\usepackage{xeCJK}
\usepackage{setspace}
\usepackage{fancyhdr}
\usepackage{hyperref}

% 设置中文字体为 Noto Serif CJK SC
\setCJKmainfont{Noto Serif CJK SC}

% 设置行距
\setstretch{1.5}

% 将目录页标题改为“目录”
\renewcommand{\contentsname}{目录}

% 超链接和书签设置
\hypersetup{
    colorlinks=true,
    linkcolor=blue,
    anchorcolor=blue,
    citecolor=blue,
    filecolor=blue,
    menucolor=blue,
    runcolor=blue,
    urlcolor=blue,
    pdftitle={三国演义},
    pdfauthor={kass},
    pdfsubject={古典文学},
    bookmarksnumbered=true,
    bookmarksopen=true,
    bookmarksopenlevel=1,
    pdfpagemode=UseOutlines,
    pdfstartview=FitH,
    hypertexnames=false
}

% 目录页使用空白样式
\fancypagestyle{tocstyle}{
  \fancyhf{}  % 清除页眉和页脚
  \fancyhead[LE,RO]{\thepage}  % 仅保留页码
  \renewcommand{\headrulewidth}{0pt}  % 取消页眉下划线
}

% 正文部分只显示“正文”两个字
\fancypagestyle{mystyle}{
  \fancyhf{}
  \fancyhead[LE,RO]{\thepage}
  \fancyhead[C]{正文}  % 中间部分只显示“正文”
  \renewcommand{\headrulewidth}{0.5pt}
}

\setlength{\headheight}{14pt}
\parskip 0.1in

\begin{document}
\frontmatter

\begin{titlepage}
\centering
{\Huge 三国演义\par}
\vspace{1.5cm}
{\LARGE 罗贯中\par}
\vfill
\newpage % 只在书名和作者之后添加一个空白页
\end{titlepage}

% 使用目录样式生成目录
\pagestyle{tocstyle}
\tableofcontents % 生成目录

% 切换到正文样式
\mainmatter
\pagestyle{mystyle}

% 从 content.txt 文件中自动生成章节内容
\input{content.txt}

\end{document}

